Function argmax(matrice):
    Max = WorksheetFunction.Max(matrice)
    argmax = WorksheetFunction.Match(Max, matrice, 0)
End Function

Function argmin(matrice):
    Min = WorksheetFunction.Min(matrice)
    argmin = WorksheetFunction.Match(Min, matrice, 0)
End Function
'NB on ne gère pas l egalite entre plusieurs elements
